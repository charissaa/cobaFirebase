<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/etProvinsi"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:hint="Masukkan Nama Provinsi"/>
    
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/etIbukota"
        app:layout_constraintTop_toBottomOf="@id/etProvinsi"
        app:layout_constraintStart_toStartOf="@id/etIbukota"
        android:hint="Masukkan Nama Ibukota"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btSimpan"
        app:layout_constraintTop_toBottomOf="@id/etIbukota"
        app:layout_constraintStart_toStartOf="@id/etIbukota"
        android:text="Simpan Data"/>
    
    <ListView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/lvData"
        app:layout_constraintTop_toBottomOf="@id/btSimpan"
        app:layout_constraintStart_toStartOf="@id/btSimpan"/>

</androidx.constraintlayout.widget.ConstraintLayout>